{
  "name": "customer-service-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Free knowledge base entry + online generator for customer service scripts",
  "scripts": {
    "build:site": "node scripts/build-kb.mjs",
    "build:offline": "node scripts/build-offline-html.mjs",
    "build:excel": "node scripts/build-excel.mjs",
    "lint:templates": "node scripts/lint-templates.mjs",
    "dev:serve": "python -m http.server 4173 --directory docs"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
